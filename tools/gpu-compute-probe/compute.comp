#version 450
layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;
layout(set = 0, binding = 0, std430) buffer Output { uint value[]; } output_data;
void main() {
    uint i = gl_GlobalInvocationID.x;
    output_data.value[i] = i * 3u + 7u;
}
