# Bluetooth controller-registration readiness — 2026-09-26

This is a host-only implementation and build record. No phone command,
physical UART, Bluetooth controller, firmware transmission, raw Bluetooth
socket, or network action was performed by this worker. Host regressions use
local PTYs and Unix socketpairs.

## Current evidence boundary

The coordinator's handoff reports the RECOVERY image SHA-256
`42da267f3dd9f94f30f62a95fb2ac13f91d4cf98f1a2307f7cc14e45d9c49be5` and
candidate GNU build ID `b2dda820b18d410d9bf12f1bd2584567d545991d`. The second
trial journal records one successful raw-HCI socket create/close check; that
operation must not be repeated. The coordinator also reports zero registered
`hci*` entries in `/sys/class/bluetooth`. Controller registration has not been
tested, and the earlier trial authorization did not include controller
attachment. These are handoff facts, not fresh observations from this branch.

The earlier raw-socket panic is not the current unresolved gate: the supplied
candidate passed its one socket create/close check. That single result does not
prove HCI controller registration or radio operation.

## Bridge lifecycle change

The embedded bridge previously opened a second `AF_BLUETOOTH` raw socket after
its 20-second bridge interval to print adapter statistics. It did so even when
the bridge had failed or received a stop signal. The bridge no longer opens
that socket. A successful `HCIUARTGETDEVICE` now supplies the registration
receipt (`bridge_registered_hci=<index>`); in the pinned
`drivers/bluetooth/hci_ldisc.c`, this ioctl returns `hu->hdev->id` only when
`HCI_UART_REGISTERED` is set, and otherwise returns `-EUNATCH`.

The bridge records ownership only after `TIOCSETD(N_HCI)` succeeds. A failed
initial line-discipline ioctl has no detach attempt; failures in
`HCIUARTSETPROTO` or `HCIUARTGETDEVICE` reach one shared detach path. Ownership
is consumed before the detach ioctl, so a failed detach is reported and never
retried. The original attach/bridge errno is restored after cleanup; a detach
failure changes an otherwise successful result to failure and is separately
reported. The standalone CLI bridge has a distinct one-time TTY restoration
path; it does not claim HCI attachment. This does not turn a host registration
receipt into phone or controller acceptance.

The runner's `--execute` guard remains closed. Its message now reflects the
consumed one-shot socket check and requires a fresh exact RECOVERY identity and
full readback, independent review of attach/detach/poweroff, and separate
explicit authorization. Its configured binary and staging destination point
to this milestone's unique paths.

## Host probe artifact

Built the current `bt-qca6490-hci-bridge-probe.c` as a static AArch64 ELF using
the existing Ubuntu cross-compiler. Its ignored private include dependencies
were read in place and not copied into this branch:
`/home/corpunum/s22-linux/builds/bt-patch-20260922/qca-patch-private.h` and
`/home/corpunum/s22-linux/builds/bt-runtime-nvm-20260922/s22_nvm_payload.h`.
No header contents, payload, address, or private-input hash are recorded here.

```sh
aarch64-linux-gnu-gcc -static -std=c11 -Wall -Wextra -Werror -O2 \
  -I /home/corpunum/s22-linux/tools/hardware \
  tools/hardware/bt-qca6490-hci-bridge-probe.c \
  -o builds/bt-next-20260926/bt-qca6490-hci-bridge-probe
```

Compiler: `aarch64-linux-gnu-gcc` 13.3.0, SHA-256
`cd90adc7801f4595267f61a5d25bd3a0c6beb2f9f1f107ab919a97a12972dc9a`.
The rebuilt output is ELF64 little-endian AArch64, statically linked, 1,042,008
bytes, mode `0700`, BuildID `eb47c232ddb7fc4b477bb145bbe9bf2832972276`, SHA-256
`c28307985bdad6404f0fecc82860eaa92a2c150a5d870d9297ce0301f44bac0a`. It is
under the ignored `builds/` path, remains local, and was neither staged nor
executed.

## Regression evidence

`python3 tools/hardware/test-bt-h4-ibs-bridge.py` and its `python3 -O` run each
passed all 21 tests. Embedded-C/PTY lifecycle cases cover clean registration
via `HCIUARTGETDEVICE`, device-index lookup failure, failed initial
`TIOCSETD`, injected `HCIUARTSETPROTO` failure, malformed UART input, exact
eight-frame queue overflow, SIGTERM, detach ioctl failure, and preservation of
the primary errno when detach also fails. They check exactly one detach after
successful N_HCI attachment, no detach after failed initial attachment, and
zero raw-HCI socket/ioctl calls. A separate runner test confirms `--execute`
still exits at the gate. The pinned socket-restore source contract passed; no
test fixture was skipped. The full host regression allowlist was not run.

## Next trial definition and prerequisites

The configured trial command is:

```sh
python3 tools/hardware/run-bt-hci-bridge-once.py bt-hci-registration-20260926 --execute
```

It currently exits unconditionally at `LIVE_TRIAL_GATE` before the hardware
runner is entered. It is not yet a runnable post-approval command. The reusable
board runner records a one-shot named receipt and checks generic health/device
metadata, but it does not gate on the exact RECOVERY image SHA/build ID, a
completed HCI observer receipt tied to the same live boot, or a current
controller-absent/system/WLAN baseline. The wrapper must gain explicit,
host-tested opt-in and those exact preflight checks before this command can be
considered for use. Do not bypass the wrapper by invoking its imported board
runner directly. Separately, the coordinator must confirm exact image
readback and candidate identity, an independent reviewer must approve the
procedure, and the owner must separately authorize one controller-registration
attempt. The original authorization does not cover this command.

Once separately authorized, the existing source path keeps this order:
QTI app-version and PatchVer identity at 115200; the source-derived 3-Mbaud
transition and PatchVer identity readback; the 806-segment RAM patch; board-ID
readback; 29 acknowledged normal-NVM segments; HCI Reset; generated-address
and local-version readbacks; then H4 attachment with userspace IBS. This work
changed no firmware, NVM, reset, address, or factory-identity path. The bounded
bridge interval is 20 seconds; the expected registration receipt is
`HCIUARTGETDEVICE`, followed by one line-discipline detach, UART restoration,
Bluetooth power-off, and a balanced Bluetooth power-vote check. Preserve the
independent WLAN vote. Stop on any error or unknown outcome; do not retry.

The attempt must not create another raw HCI socket, bring the HCI controller
up, scan, advertise, pair, or read a factory address. Coordinator read-only
post-checks may confirm the registered sysfs entry and ordinary health while
the bridge is active. Registration alone would not establish radio, pairing,
or Bluetooth-audio acceptance.
