# S22 remaining-driver implementation — 2026-09-26

Continuation of `s22/luna-driver-completion-20260923` at `2a4f3d171627d41cb703af880a33521c4c964245`. Fetch found no newer remote commits. The original dirty checkout and all historical worktrees are preserved; master is not the integration target.

## Live starting point

Coordinator read-only USB SSH inspection identifies the r0s HCI candidate by GNU build ID `b2dda820b18d410d9bf12f1bd2584567d545991d` and newest RECOVERY boot record. Uptime was 183030 seconds. Native guardian, desktop, desktop Pi, model health/idleness, browser terminal, networking, and 325 loaded modules were healthy. Dedicated Pi tmux is absent in the documented on-demand state. Battery was Full/100%, 27.1 C, maximum thermal reading 42 C. The complete available dmesg ring contained zero fatal indicators and zero hung-task warnings; full-boot log coverage and TrustZone progress remain unproven.

The completed second trial journal records one candidate RECOVERY write, one recovery reboot request, one successful raw-HCI create/close, and no rollback. Its operation lock was released. Those operations are consumed and are not reissued in this wave. Independent hardware rescue remains unproven. No new phone mutation has occurred in this wave.

A further read-only inventory found no registered `hci*` controller, modem state `INIT`, and PCM2 playback `closed` with ABOX runtime `suspended`. Camera pipeline/codec nodes are enumerated but no camera was opened or frame captured. The root overlay has 34,844,672 free bytes and 29,780 free inodes. `/srv/s22` and the Arch mount share the persistent filesystem, with 101,778,141,184 free bytes and 1,652,499 free inodes. Root-overlay scarcity does not prevent appropriately scoped staging on that persistent filesystem; no cleanup or package installation was performed.

## Actual workers

The supported collaboration interface accepted explicit `model=gpt-6-luna`, `reasoning_effort=max` selections. Evidence level is **explicitly configured**; the spawn response provides task handles, not independent backend model attestation. There is no known override/fallback. Three workers plus the coordinator fill the four available concurrent slots. Only the coordinator accesses the phone, integrates, and publishes.

| Actual task handle | Worktree / branch suffix | Scope and deliverable | State / tests / commit |
| --- | --- | --- | --- |
| `/root/bt_next_20260926` | `/home/corpunum/s22-workers/bt-next-20260926`, `codex/s22-bt-next-20260926` | Removed the bridge's second raw-HCI query; exactly-once detach ownership; actual-C/PTY regressions; private ARM64 probe built; explicitly gated trial adapter | Author `215dbd1` / `38484d1` / `18cef2b` / `4781e5c` / `f6a7bf7` / `a29ad37`, integrated as `ff40b43` / `7d62434` / `530101a` / `421eaee` / `b891073` / `a4ab651`. Bridge 21 and adapter 17 tests normal and optimized. Independently approved host-only; no device execution. |
| `/root/audio_next_20260926` | `/home/corpunum/s22-workers/audio-next-20260926`, `codex/s22-audio-next-20260926` | Corrected source-derived DPCM/DAPM paths and fixed UAIF1 clock inventory; synchronized stage classifier and candidate gate | Author `1cc6013` / `851f27d`, integrated as `5e43109` / `375e3a9`; final normal and optimized route 33, snapshot 21, wrapper-cleanup 4 tests passed. Independently approved host-only after corrections; no stream operation. |
| `/root/npu_next_20260926` | `/home/corpunum/s22-workers/npu-next-20260926`, `codex/s22-npu-next-20260926` | Actual candidate C-helper race harness and separate teardown/readiness limits; lifecycle patch unchanged | Author `68ae671` / `8d72aa8`, integrated as `c3a3a1e` / `97ba6f3`. Lifecycle/preflight suites passed normal and optimized. Reviewer-approved host harness; unavailable-source gap fields corrected to unknown. No NPU kernel build/runtime action. |
| `/root/driver_review_20260926` | `/home/corpunum/s22-workers/review-next-20260926`, `codex/s22-independent-review-20260926` | Independent BT/NPU review, focused audio correction review, and minimal host-CI integration; no author-source edits or phone access | BT cleanup and NPU corrections approved host-only. Audio corrections approved after independent 33/21/4 normal and optimized tests. CI author `2907181`, integrated as `05fe6de`. BT adapter review pending. Explicit `gpt-6-luna/max`. |
| `/root/npu_next_20260926` (second assignment) | `/home/corpunum/s22-workers/audio-review-next-20260926`, `codex/s22-audio-independent-review-20260926` | Independent audio review in a separate worktree; this worker did not author audio changes | Completed source/gate review and reproduced two classifier gaps; author corrected both. A subsequent resume hit the runtime thread limit, so the already-running independent reviewer reviewed the narrow correction; no substitute model was launched. |
| `/root/driver_review_20260926` (adapter assignment) | `/home/corpunum/s22-workers/bt-adapter-review-20260926`, `codex/s22-bt-adapter-review-20260926` | Independent review of the committed Bluetooth adapter and regressions | Artifact-consumption race corrected; synthetic-fixture replacement regression accepted. Adapter 17/17 and policy 7/7 independently passed normally and optimized. CI author `1016a6d`, integrated as `1bac739`. Host-only approval; no live clearance. |

Workers own disjoint driver files and one research note each. They may edit and test host source, but may not access the phone, publish, change shared deployment/observer/CI files, or start heavy kernel builds. Independent review and CI integration use the next available worker slot. Subsequent work will address cellular/camera, GPU/presentation, input/power, and other hardware using their existing source and evidence.

Host tests, source/build evidence, live driver operation, and physical acceptance are reported separately. Firmware, images, weights, credentials, raw device traces, and network identifiers stay private.

## New diagnostic findings

The coordinator ran one bounded idle audio inventory through the pinned SSH transport, without opening PCM, mixer controls, or ABOX registers. It confirmed PCM closed, ABOX suspended, cache-only enabled, service available and reset count zero. The old RDMA2/UAIF1 DAPM and DPCM paths were absent; SPEAKER/RECEIVER reported Off. The generic clock inventory reached its 12-entry limit and explicitly reported truncation. It did include the UAIF1 divider, whose cached rate/enable count were zero at idle. This does not identify a streaming failure or establish a physical clock state. The pinned `drivers/clk/clk.c` debugfs rate reader returns cached `core->rate`; the count files expose stored counters and do not invoke provider MMIO. The audio worker is reusing the previously source-derived widget inventory from the preserved original checkout.

The NPU trace showed why a heap waiter alone cannot fix the publication drain: after a timed-out caller returns, a publisher may still enter the mailbox while boot-error cleanup turns off hardware or closes protocol/interface resources. The worker is preserving the existing lifetime-safe drain while adding tests that execute the extracted C helpers. Any future bounded-detach change must prove ownership of device/protocol lifetime and the selected hardware references, not just waiter memory; BOOTUP remains refused.

## Independent review findings

The Bluetooth reviewer independently passed the initial 17 normal and optimized tests, reproduced the old post-loop raw-socket query with the new actual-C harness, and verified the private generated probe and firmware/NVM dependency receipts without publishing their contents. Review also injected HCIUARTSETPROTO failure: local rollback plus common cleanup caused two detach calls. The author corrected ownership and added four regressions, then rebuilt the private artifact; that correction is under independent review. Controller registration remains untested.

The NPU reviewer independently passed both lifecycle/preflight suites normally and optimized, and confirmed that the unchanged candidate patch applies to the clean pinned kernel. The actual-C harness executes extracted production helpers with pthread shims, not kernel MMIO or firmware. Review requested that unavailable lifecycle sources report unknown gaps rather than positive missing-code findings; the author supplied that correction without weakening BOOTUP refusal.

Independent audio review reproduced a new false-localization case: RDMA movement and stationary ALSA pointers from non-overlapping sample windows were compared. It also reproduced a pre-existing gap in the neighboring DMA assessment, which could ignore missing sample sequences. The author corrected both and added negative tests; the second independent reviewer approved those corrections. Separately, the legacy live audio runner has only a name-exclusive local directory, not a global operation lock, and its remote strace destination is not reserved exclusively. These execution limitations must be resolved before calling its proposed stream command ready. The fixed CI allowlists now include the independently accepted audio assessment and cleanup suites; reviewer policy tests passed 7/7 and the 17-script suite passed normally and in its 14 supported optimized runs, with three existing AVB tool-dependent skips in each mode.

Bluetooth source review found that HCI registration queues asynchronous controller setup and can set HCI_UP without an explicit userspace HCIDEVUP request. A registration trial therefore is not a quiet, command-free handoff. Procedure wording and any future authorization must cover automatic initialization; the previous raw-socket-only authorization did not cover attachment. No attachment is attempted by this wave.

The Bluetooth adapter pins the artifact/source and trusted host transport, then checks the completed prior observer receipt, same current boot, exact build ID/full RECOVERY hash, native/model/WLAN health and absence of a registered controller. Optimized host execution is refused; the remote stage is forced into isolated Python so inherited optimization cannot disable its legacy assertions. The regression executes the actual remote stage on a temporary host filesystem with controlled identity rather than mocking a successful SSH reply. Independent review also found that the legacy board runner re-read the artifact after validation and derived a fresh unconstrained digest; the author added a final check of the actual immutable staging input against the approved artifact hash/size and a replacement regression. This does not establish live clearance: kernel detach has no overall deadline, and a shared general device-operation lock is not yet provided. A timeout means UNKNOWN, not verified cleanup or rescue.

Clean-worktree review then caught a test-only dependency on the ignored probe binary. The author replaced it with a generated synthetic AArch64 ELF. The final replacement test keeps byte length unchanged and flips one byte after preflight, then runs the actual board runner with controlled host/transport operations. This specifically tests SHA rejection, not merely a size mismatch. No private probe, firmware header contents, or payload is included in the tests or publication.

## Final read-only recheck so far

The coordinator independently rehashed both exact 100,663,296-byte host images and the phone's full RECOVERY partition. The final read-only check at 188,563.93 seconds uptime still matches the HCI candidate's full hash and runtime GNU build ID, and the reviewed native/service/network/power snapshot gate passed. Battery was Full/100%, 27.2 C, maximum thermal reading 41 C. The 255,806-byte available log had no fatal, hung-task or call-trace indicators; this is not full-boot coverage or proof of TrustZone progress. A sanitized receipt is in `evidence/s22-driver-readonly-20260926.json`. No previous trial marker was reset or reused.

After independent source review, the coordinator executed the corrected idle-path inventory with register reads disabled and clock reads disabled. All 24 fixed DAPM widget paths were readable and Off. The corrected DPCM path was readable and reported RDMA2 state `new`, with no active DSP links. PCM remained closed and ABOX suspended/cache-only with service available. This verifies diagnostic path availability only; no stream, route write, register read, or physical-clock observation occurred.

## Integrated verification and next concrete gates

At code commit `1bac73960ac81dcfc5de81a03eaca6b1f14b0a84`, the coordinator ran the complete fixed allowlist: 18 normal script passes, 15 optimized script passes, three documented optimization skips, zero failures. The deployment suite explicitly skipped three unavailable AVB-tool fixtures per mode. Runner-policy tests passed 7/7 in both modes. Details are in `evidence/s22-driver-host-tests-20260926.json`. New history was inspected for publication: only source, tests, documentation, and sanitized receipts; no firmware, binaries, model weights, credentials, or private traces. Master and the dirty original checkout are not publication targets.

- Bluetooth: resolve general trial serialization, then obtain separate authorization for one registration/automatic-init attempt with explicit unknown-outcome handling. The previous raw-socket authorization is consumed and excludes attachment. No scan/pairing is implied.
- Audio: resolve global serialization and exclusive remote trace reservation before the one bounded, muted digital-zero diagnostic. The new live-verified paths are available; actual DMA/backend/clock progress is not yet captured.
- NPU: establish publisher/device lifetime and shutdown ownership before changing the drain; retain BOOTUP refusal. Host C helper tests are not a hardware readiness gate.
- Cellular, cameras, resident-4B/desktop GPU acceleration, and physical input/power acceptance remain separate unfinished work; idle inventory does not establish functionality.

No new kernel was built or deployed in this wave. The existing candidate and rollback images are preserved. Publication/hosted-CI status is verified separately against the pushed branch SHA rather than inferred from these local results.
